# Retired: crons deploy from AbstractPlay/node-backend (crons/) on develop push and dep_update_dev.
name: Deploy Dev (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Use node-backend monorepo
        run: |
          echo "::error::This repository is archived. Deploy the abstract-play-backend-crons stack from https://github.com/AbstractPlay/node-backend (crons/scripts/serverless-deploy.sh) via node-backend Deploy Dev."
          exit 1
