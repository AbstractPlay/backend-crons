# Retired: use node-backend job test-crons (lint, vitest, layers).
name: Test (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Use node-backend monorepo
        run: |
          echo "::error::This repository is archived. Run npm run test:crons and npm run test:crons:layers in https://github.com/AbstractPlay/node-backend."
          exit 1
